export const metadata = {
  title: "MAX CFO AI",
  description: "Seu CFO Virtual",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
